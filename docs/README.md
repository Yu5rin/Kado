# 資料

| ファイル | 位置づけ |
|---|---|
| `SlideinaCalendar-requirements-v1.0.md` | **仕様の正。** 迷ったらこれに従う |
| `mock-window.html` | ウィンドウモードの UI モック（参考） |
| `mock-sidebar.html` | サイドバーモードの UI モック（参考） |

## 食い違いの調査結果

要件書とモックを項目ごとに突き合わせた記録。実装で参照するときの注意点。

### 要注意：要件書の中で食い違っている

**半期ビューを設けるか。**

| 箇所 | 記述 |
|---|---|
| 5.1 ビュー構成 | 「上期・下期は行のグルーピングで示すため、**半期ビューは設けない**（ストリップに吸収）」 |
| 5.2 のツールバー図 | `│月 週 日 半期 年│` |
| 5.2 の3ペイン図 | `（月／週／日／半期／年）` |

**5.1 の方針が正。**「設けない」と理由つきで明示されている一方、5.2 は構成を示す ASCII 図で、
半期ビューを廃止する前の名残とみられる。ビュー切替は月・週・日・年・一覧の5つにする。

モック側は正しく、`mock-window.html` のビュー切替セグメントに半期は無い。
別途「比較用：旧 半期ビュー」というボタンが残っているが、旧案との対比のために
意図して置かれたもの。

### 解消済み：実働日の通し番号をセルに出すか

**要件書 4.3 が正。月ビューのセルと年ビューには出さない。** マイルストーン表示と
競合し、密度が上がりすぎるため。出す場所はツールバーのサマリー・右ペインの選択日
ヘッダ・週ビューの日付ヘッダ・一覧ビューの日付列の4か所。

`mock-window.html` は初めからこの方針どおりだったが、`mock-sidebar.html` だけが
古い案のままセル右上に番号を置いていた。要件書に合わせて直してある。

### 突き合わせて一致を確認した項目

| 項目 | 要件書 | 結果 |
|---|---|---|
| ビュー構成 | 5.1 月・週・日・年・一覧の5つ | 一致（モックに半期は無い） |
| 年ビューの範囲 | 5.1 年度単位（4月〜翌3月） | 一致 |
| 年ビューの2形式と切替 | 5.1 ストリップ／カレンダー、ビュー内のアイコンで切替 | 一致 |
| 一覧ビューの畳み込み | 5.1 予定のない日と休みを連続分でまとめる | 一致 |
| 右ペインの構成 | 5.2 予定とタスクを上下に同時表示（タブにしない） | 一致 |
| 左サイドパネル | 5.2 折りたたみ可能 | 一致 |
| サイドバーの幅 | 5.3 約352px | 一致 |
| タイムブロッキング | 5.4 終日レーン、点線枠で予定と区別 | 一致 |
| デザイントークン（ライト） | 5.6 12項目 | 全一致 |
| デザイントークン（ダーク） | 5.6 12項目 | 全一致 |
| マイルストーン配色 | 4.2 4種×（面・文字・棒）= 12色 | `mock-window.html` と全一致 |
| 実働日計算パネル | 4.5 常設せずボタンで開く | 一致 |
| 期限の表記 | 4.4 の5パターン | 実装が全パターンを出す（テスト済み） |

### 参考：モックの作り込みが仕様に届いていない箇所

仕様の食い違いではなく、モックが描いていないだけのもの。実装では要件書に従う。

- `mock-sidebar.html` にマイルストーンの表示が無い。要件書 4.2 の表示位置は
  月・週・日・一覧・年・右ペインを挙げており、サイドバーモードでも選択日ヘッダに
  ラベルを出すのが筋

## 要件書のトークン表に載っていない色

要件書 5.6 の表は主要なトークンだけを挙げている。モックはほかにも色を使っており、
実装ではモックの値を採る。

| 用途 | モックの変数 | ライト | ダーク |
|---|---|---|---|
| 一段沈めた面（ホバー、前後の月） | `--bg-sunken` | `#f1f2f5` | `#15181e` |
| 予定のカテゴリ色（緑） | `--ok` | `#2f7d5b` | `#5fb08a` |
| 予定のカテゴリ色（琥珀） | `--amber` | `#c2762b` | `#d59a55` |
| 完了したタスクの文字 | `--done` | `#a3a9b5` | `#5d6472` |

`--desk`（モックの外側の背景）はモック用の色なので実装しない。

## モックから写した月ビューの寸法

要件書には書かれていないが、モックの月グリッドが持っている表示。実装はこれに合わせた。

| 要素 | モック | 内容 |
|---|---|---|
| マス | `padding 3px 5px 4px` / `gap 2px` / `overflow hidden` | 溢れは切る |
| 日付 | `12px` / `weight 500` / 等幅数字 | 前後の月は日付だけ `opacity .4` |
| 祝日名 | `.hol` `10px` / 日曜色 | 日付の下に出す |
| マイルストーン | `.ms` `10px` / `weight 600` / `padding 0 5px` / `radius 3px` | 日付の右で中央寄せ |
| 予定 | `.chip` `11px` / `padding 1px 5px` / `radius 3px` / 左に `3px` の帯 | 「09:00 計画レビュー」と時刻を頭に付ける |
| タスク | `.chip.tk` 丸印 `5px` / 枠 `1.5px` / `gap 5px` | 帯を持たず丸印で区別 |
| 溢れ | `.more` `10px` / 補助色 | 「＋2」で件数を示す |
| ホバー | `.cell:hover` | 面を一段沈める |

前後の月のマスは面を `--bg-sunken` に落とし、**日付の数字だけ**を薄くする。
セル全体を薄くすると予定まで読めなくなる。

## モックから写したウィンドウ全体の寸法

Phase 3 のウィンドウは `mock-window.html` の CSS をそのまま寸法の根拠にしている。

| 部位 | モック | 実装 |
|---|---|---|
| ツールバー | `.tbar` `padding 9px 12px` / 下に `1px` | 同じ |
| 月の見出し | `.month` `19px` / `weight 600` / `min-width 118px`、年は `14px` `--ink-3` | 同じ |
| アイコンボタン | `.ib` `28x28` / `radius 5` / ホバーで面を沈める | `IconButtonStyle` |
| 「今日」 | `.gbtn` `h28` / `padding 0 11` / 枠 `1px` | `GhostButtonStyle` |
| ビュー切替 | `.seg` 外枠 `1px` / `radius 5` / ボタン `h28` `padding 0 13`、選択中はアクセントで塗る | `SegmentButtonStyle` |
| 実働日バッジ | `.wdchip` 面 `--ai-soft` / `radius 5` / `padding 3 10`、数字は `14px` でアクセント色 | 同じ |
| 検索 | `.search` `h28` / `w190` / 枠 `1px` / `radius 5` | `SearchBoxStyle` |
| 左パネル | `.side` `216px` | 同じ |
| ミニ月暦 | `.mg b` `10.5px` / `padding 2.5px 0` / `radius 3`、今日は塗り・選択日は内枠 | `MiniDayStyle` |
| カレンダー一覧 | `.ck` 色見本 `11x11` / `radius 3` / `gap 8`、外すと枠だけになる | `SourceListRowStyle` |
| 右ペイン | `.detail` `296px` | 同じ |
| 日付ヘッダ | `.dhead` `padding 11 14 9` / 日付 `16px` / 補助行 `11.5px` | 同じ |
| 予定の行 | `.ev` 時刻 `38px` 右寄せ＋帯 `3px`＋タイトル `13px`＋補助 `10.5px` | 同じ |
| タスクの行 | `.tk` チェック `15x15` / 枠 `1.5px` / `radius 3.5`、済みは取り消し線 | `TaskCheckBoxStyle` |
| 追加ボタン | `.add` 枠 `1px` / `radius 5` / `padding 6` | `AddButtonStyle` |
| 凡例バー | `.hintbar` `11px` / `padding 6 14` / 面 `--bg-sunken` | 同じ |

### モックと変えた3点

いずれもモックの見た目を保ったうえで、WPF 上で破綻しないようにした判断。

- **右ペインの予定とタスクをひとつのスクロールにまとめた。** モックは節ごとに
  独立したスクロール領域（`.dlist` が2つ）だが、WPF で高さを等分すると予定が
  少ない日に大きな空きができる。節が縦に続く見え方は変えていない
- **実働日バッジを「実働 N 日」と「残り N」の2つに分けた。** 残りは今日を含む月
  でしか出せないため。別の月を見ているときはバッジがひとつになる
- **まだ実装していないビューのセグメントは無効表示にした。** モックの
  `.seg button[disabled]` の見た目をそのまま使っている。押せるのに何も起きない
  よりは、押せないと分かるほうがよい

### モックに無いが実装で足したもの

- **元に戻す・やり直しはツールバーに出さず、Ctrl＋Z／Ctrl＋Y で受ける。**
  モックのツールバーにボタンが無いため、見た目を増やさずに機能だけ残した
- **同期の表示（`.sync`）は状態表示を兼ねる。** Google 同期は Phase 5 なので、
  いまは「Google 未接続」と出し、操作の結果があるときはそちらを出す。
  画面下に別のステータスバーを足すとモックの構成から外れる

### 左パネルの一覧の出どころ

モックの「カレンダー」「タスクリスト」は、予定の `calendar_id` とタスクの
`task_list_id` から重複なく引いて組み立てている。カレンダー自体の表は作っていない。
Google 側のカレンダー一覧をまだ取り込んでおらず、ローカルに独立した表を持つと
同期を始めたときに二重管理になるため。色見本は名前から決まるので、同じ名前には
常に同じ色が付く。

## 編集画面（予定・タスク）

要件書にもモックにも編集画面は無い。ツールバーや右ペインで使っている部品の寸法
（枠 `1px` ／ 角丸 `5` ／ 高さ `28` ／ 見出し `11px` 補助色）に合わせて組んである。

項目は **Google Calendar / Google Tasks に合わせてある**。Phase 4 で同期を始めたときに、
こちらにしか無い項目・あちらにしか無い項目が出ないようにするため。

| 画面の欄 | 予定（Google Calendar） | タスク（Google Tasks） |
|---|---|---|
| タイトル | `summary` | `title` |
| 日付 | `start` / `end`（終日は日付、時刻付きは日時） | `due`（**日付のみ**。時刻を持たない） |
| 繰り返し | `recurrence`（RRULE） | なし |
| 場所 | `location` | なし |
| URL | `source.url` | なし |
| 説明・詳細 | `description` | `notes` |
| 所属 | `calendarId` | タスクリスト |
| 完了 | なし | `status` |

Google Tasks の期限が日付だけなので、タスク側に時刻欄は置かない。

**色の欄は置かない。** 所属カレンダーで決まる（`ICalendarPalette`）。1件ずつ選ばせると、
左パネルの色見本と画面上の帯が食い違う。取り込んだ予定が持っている色は、上書きせず
そのまま残す。

### まだ合わせられていない項目

いずれもデータ層に列が無く、同期を始める Phase 4 以降でないと意味を持たない。

| Google 側 | 用途 | いまの扱い |
|---|---|---|
| `reminders` | 予定の事前通知 | **無し。** 通知そのものが Phase 6（要件書 7.5） |
| `attendees` | ゲスト | 無し |
| `conferenceData` | Meet のリンク | 無し。URL 欄で代用できる |
| `transparency` / `visibility` | 予定あり／公開設定 | 無し |
| Tasks の `parent` | サブタスク | 無し |
| Tasks の繰り返し | タスクの繰り返し | 無し（Tasks API が公開していない） |

保存できない間はボタンを止め、**理由を下に出す**。押せない理由が分からないのが一番困る。

### 入れやすさのための作り

1日に何度も触る画面なので、打鍵を減らすことを優先した。

- **時刻はゆるく読む。**「9」「930」「9:30」「０９：３０」のどれでも入る（`TimeInput`）。
  コロンと 0 埋めを毎回求めない
- **候補からも選べる。** 15分刻みの一覧を出す。終了側は
  「10:30（1時間30分）」と長さを添える。「何時まで」より「何時間か」で決めることが多い
- **上下キーとホイールで15分ずつ動く。** 一覧が開いているときは候補送りに譲る
- **開始を動かすと終了も同じ長さのまま付いてくる。** 終わりを打ち直させない
- **日付欄は中身ぶんの幅に留め、時刻と横に並べる。** 複数日のときは開始日と終了日を横に並べる
- **タスクの期限は「今日・明日・来週」から早入れできる。** 期限なしのタスクに使うと期限も付く

### 繰り返しの扱い

選択肢は「繰り返さない・毎日・毎週◯曜日・毎月◯日・毎年◯月◯日」の5つ。開始日に
合わせて文言が変わる。

取り込んだ予定が「隔週の月水金」のような選択肢で表せない指定を持っていることがある。
これは **`カスタム` として元の文字列をそのまま持ち続ける**。選択肢に無いからと消すと、
タイトルを直して保存し直すだけで繰り返しが失われる。

編集画面で触っていない項目（繰り返し、Google 側の ID と更新時刻、取り込み元）は
**そのまま引き継ぐ**。保存し直すたびにこれらが消えると、同期を始めたときに壊れる。

操作は右ペインから。行をダブルクリックで編集、指した行にだけ出る削除ボタン、
チェックで完了の切り替え。月ビューのマスは1回押しで選択、2回で予定の追加。
どれも `CalendarWorkspace` を通すので Ctrl＋Z で戻せる。

### 右ペインのタスクは「その日のぶん」ではない

未完了で期限のあるタスクを**期限の早い順にすべて**出し、完了済みは選択日が期限のものだけ
添える。その日のぶんしか出さないと、今日やることは分かっても段取りが組めない。モックの
右ペインも 10/1 や 2027/3/31 期限のタスクを並べている。

見出しの「3 / 4」は**残っている数と全体**。片付いた数より、あと何件あるかのほうが知りたい。

### WPF で踏んだ落とし穴

いずれもビルドは通るのに実行時だけ壊れるもの。

- **`StaticResource` は型変換を挟まない。** アイコンを `sys:String` で置いて
  `Path.Data` に渡すと `Geometry` にならず、読み込み時に落ちる。`PathGeometry` で持つ
- **`InputBinding` の中では `RelativeSource` で祖先をたどれない。** `InputBinding` は
  視覚ツリーに居ないため。マスや行のダブルクリックはコードビハインドで受けている
- **`RelayCommand` は `CommandManager` に乗っていない。** `RaiseCanExecuteChanged` を
  自分で呼ばないと、保存できるようになってもボタンが無効のまま戻らない

`tests/SlideinaCalendar.App.Tests` が1つ目を静的に検査する。2つ目と3つ目は
検査できていないので、同じ書き方をしないよう上に残した。

### まだ手を付けていないところ

- **日付欄のポップアップは WPF 既定の暦のまま。** 外側の枠と高さは周りに揃えたが、
  開いたあとの暦は既定の見た目。ここを作り替えるのは別の仕事として残してある
- 設定（⚙）と実働日計算のボタンは、まだ何も開かない

## 取り込みの呼び出し口

ツールバーの ⚙ から開くメニューに置いた。

| 取り込むもの | 形式 | 重ね方 |
|---|---|---|
| 配布の実働日ファイル | Excel | **期間単位で置き換える。** 古いファイルを誤って読み込んでも、その期間の外は消えない（要件書 4.1） |
| 旧 inaCalendar のバックアップ | JSON | 同じ識別子の予定とタスクを上書き。実働日は既存と併合する |

どちらも **Undo には積まない**。まとめて書き込むので、1手で戻せる単位にならない。
旧データの取り込みは元に戻せないので、先に断りを入れてから選ばせる。

読めないファイルを選んでも落とさない。何が起きたかを結果の窓に出して続ける。

## 通知について

**通知は Phase 6 で、通知を出す仕組みごと作る。** それまで編集画面に通知の欄は置かない。

Google Calendar のイベントは `reminders` を持つので、項目としては揃っていない。
ただ、欄だけ先に付けると「設定したのに鳴らない」状態になる。欄が無いより悪い。

## その他、要件書で方針が定まっているもの

- **ビュー別の表示項目 ON/OFF は廃止する**（5.5）。旧データの `settings.show` と
  `settings.fontSize` に相当する設定は作らない。レイアウトの作り込みで解決する
- 設定は残すものが明示されている（5.5）。テーマ、週開始曜日、表示時間帯、通知、
  カテゴリ管理、カレンダーの表示切替と並び順、実働日データの供給元、起動時の表示モード、自動起動
