# Kado の進め方

## 配布の手順

全体の決まり（`~/.claude/CLAUDE.md` の「リリースの進め方」）に沿う。Kado で
中身が決まっているところは次のとおり。

「ビルドして」と言われたら、**タグを打つコマンドを渡すところまでを1回で出す**。

1. **手順を確かめる**：この節と「リリース」の節を読む。Kado の事情は次のとおり
   - 版を書くファイルは無い。版はリリースワークフロー（`release.yml`）の入力で渡し、
     発行のときに `-p:Version` で焼き込む。**版を上げる PR は作らない**。CHANGELOG も無い
   - `release.yml` のきっかけは2つ。`dev` での手動実行（`workflow_dispatch`）は
     ビルド・テスト・発行をして成果物を残すだけ。タグ `v*` の push で Releases に
     **下書き**を作る（すぐには公開しない）
   - 作業は `dev` に直接積む。リリースの前にマージするものは無い
2. **版を決めて、ビルドとテストを通す**
   - 前回のタグ以降のコミットを見て、次の版を提案する。既定は
     **3つめの数字を1つ増やす**（1.0.1 の次は 1.0.2）。2つめを増やすかどうかは
     その都度ユーザーが決める
   - `dev` でリリースワークフローを走らせ、成功を確かめる。ログで Google の
     接続設定が焼き込まれたこと（`CLIENT_ID: ***` / `CLIENT_SECRET: ***`）と、
     ビルドした版が `dev` の最新であることも見る
3. **リリース本文の案を作る**（「リリース」の節の形。更新の窓が本文を読むことに注意）
4. **タグを打つコマンドを渡す**：下の PowerShell を、版を埋めて1つのコードブロックで
   渡す。Windows PowerShell 5.1 で動く形にする
5. **タグの push と公開は、選択肢をダイアログで出し、ユーザーが選んでから**進める
6. **下書きができたら**、添付（`.exe` と `.zip`）・SHA256・本文を確かめて報告する。
   公開はユーザーが行う。公開後は `releases/latest` が新しい版になったことを確かめる

タグは Claude Code のセッションからは push できない（ブランチは通るが
`refs/tags/*` は 403 が返る）。**必ず手元で打ってもらう。**

ビルドの成果物（Artifacts）と Releases は別物。更新機能は Releases の最新版を
見に行くので、**タグを打つまでアプリは新しい版に気づかない。** 成果物だけ渡して
「更新が来ない」となったことがある。

```powershell
cd $HOME
Remove-Item -Recurse -Force kado-tag -ErrorAction SilentlyContinue
git clone https://github.com/Yu5rin/Kado.git kado-tag
cd kado-tag
git checkout dev

git -c user.name="Yu5rin" -c user.email="Yu5rin@users.noreply.github.com" tag -a vX.Y.Z -m "Kado X.Y.Z"
git push origin vX.Y.Z
```

`-c` で名前とメールをその場で渡している。注釈付きのタグ（`-a`）は作り手の
名前とメールが要るので、git を設定していない端末だと「Committer identity
unknown」で作れず、続く push も「src refspec … does not match any」で落ちる。
`-c` はその1回だけ効くので、端末の設定は変えない。メールは GitHub の
非公開用アドレスにして、実際のアドレスを出さない。

## リリース

説明は README、リリースは変更点だけ。

- **README に置く**: アプリの説明・主な特徴・動作環境・インストール・更新方法・
  使い方の入口・ビルド。版をまたいで変わらない説明はすべて README。書くのは現在の事実だけ
- **リリース本文**: 「## 変更点」と「## ダウンロード」（ファイル・サイズ・SHA256 の表）、
  最後に README への案内1行（`インストールと更新の方法は [README](https://github.com/Yu5rin/Kado#インストール) をご覧ください。`）。
  `##` の見出しはこの2つだけで、その版の小見出しは `###` 以下。
  その版に上げるときだけ要る注意（データの引っ越し、手でやることなど）は `###` で変更点の中に置く
- 本文に「# Kado vX.Y.Z」のような題名は書かない。**リリースのタイトルはタグと同じ表記**（`v1.0.5`）
- 変更点は利用者向けの日本語で書く。コミットの件名や「Full Changelog」のリンクをそのまま
  載せない。内部の名前（クラス名など）は出さない
- `release.yml` は本文の骨組み（変更点は「(ここに書く)」）とダウンロードの表を作り、
  **下書きで**リリースを作る。変更点を書き入れてから公開する。下書きは `releases/latest` に
  出ないので、公開するまで更新の窓には出ない（以前はすぐ公開していたため、書き換えるまでの間
  「(ここに書く)」が利用者に見えていた）

**更新の窓は本文を読んでいる**（`ReleaseFeed.Parse` が `body` を `ReleaseNotes` に入れ、
`UpdateWindow` がそのまま出す）。条件は次のとおり。

- 本文は Markdown として解釈されず、文字のまま出る。見出しの `##` や表の `|` もそのまま見える
- SHA256 の照合は本文を見ない。GitHub が添付ファイルごとに持つ `digest` を読む
  （`ReleaseFeed.ReadSha256`）。だから本文の表は人が確かめるためのもので、形を変えても更新は壊れない
- 更新が落とすのは添付のうち最初の `.exe`。表の並びではなく添付の並びで決まる

## 旧い名前（SlideinaCalendar）

アプリもリポジトリも Kado に改めた。ただし次の2つは**旧い名前のまま**で、
変えてはいけない。

1. データの保存先の引っ越し元。`CalendarDatabase.LegacyFolderName` が
   `%LOCALAPPDATA%\SlideinaCalendar` を指している。0.9.15 より前から使って
   いる人の予定・タスク・設定・Google のトークンはそこに入っている。
2. `WorkdayFeed` が書き出す `"app": "inaCalendar"`。配信ファイル（feed.json）の
   形式を旧い道具と共有しているため。

公開ページの置き場所は `Yu5rin/kado-site` に改めた。ここを変えると GitHub Pages の
URL が変わるので、Google のブランディング欄（ホームページ・プライバシーポリシー）と
Search Console の所有確認も一緒に直すこと。詳しくは `site/README.md`。

Google の同意画面に出るアプリ名は Google Cloud のブランディング欄にあり、
こちらのコードとは別に持っている。変えるならそちらで直す。

## Google のカレンダーとタスクを壊さない

**いちばん優先する決まり。** 使う人の予定とタスクは、この道具の外に実体がある。
こちらの都合で失わせてはいけない。機能が1つ足りないより、1件の予定が
消えるほうがずっと重い。

書き込むときの決まり。

1. **分からないものは送らない。** こちらで表せない指定（RDATE や RRULE 2本以上の
   繰り返しなど）を受け取ったときは、その項目を PATCH に載せない。控えた生データを
   持っているだけにして、向こうの値に触らない。`EventMapper.HoldsUnrepresentableRecurrence`
   がその形。
2. **消す操作は、消す意思があるときだけ。** 空の値・空の配列は、Google では
   「消してくれ」という意味になる。「持っていない」と「消したい」を取り違えない。
3. **送れないものは、送れないと分かるようにする。** 読み取り専用のカレンダー、
   他人が主催する予定、Google 側で編集できない予定は、こちらでも編集させない。
   黙って食い違わせない。
4. **上書きする前に知らせる。** 同期の衝突で向こうを採るときは、何を捨てたかを
   `SyncReport.Warnings` に出す。
5. **移すときは、移す API を使う。** 消して作り直すと、こちらが持っていない項目
   （ゲスト・会議 URL・添付・色）が落ちる。

読むだけの機能は、この決まりの上では安全。迷ったら「読んで出すだけ」から始める。

## 実機で確かめること

WPF は Linux では動かせないので、CI で分かるのはコンパイルが通ることまで。
起動時にだけ壊れる書き方は `docs/README.md` にまとめてある。書く前に読むこと。
