using System.Windows.Controls;

namespace SlideinaCalendar.App.Views;

/// <summary>月ビュー。表示だけを担い、状態は MonthViewModel が持つ。</summary>
public partial class MonthView : UserControl
{
    public MonthView() => InitializeComponent();
}
